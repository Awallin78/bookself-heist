module.exports = {
  plugins: {
    // Newer Tailwind versions separate the PostCSS plugin into '@tailwindcss/postcss'.
    // Install it with: npm install -D @tailwindcss/postcss
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
